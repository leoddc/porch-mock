# Mock Endpoint

This is a mock endpoint which responds with a list of `n` fake addresses in the form:

```json
[{"firstName":"Natalia",
"lastName":"Marks",
"donatingFood":true,
"donatingMoney":true,
"location":{}}
```
